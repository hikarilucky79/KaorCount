DATABASE_URL=mysql+pymysql://root:senha@localhost:3306/kaorcount
SECRET_KEY=colaca-sua-vagabundo
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
API_V1_PREFIX=/api/v1
FATSECRET_CLIENT_ID=sua-client-id
FATSECRET_CLIENT_SECRET=seu-client-secret

# Auth0 (opcional). Se preenchido, o backend passa a aceitar Access Tokens
# RS256 emitidos pelo Auth0 (Validado contra o JWKS público do tenant) em
# /auth/me, com provisionamento JIT do usuário pelo "sub". Seu Domain
# precisa estar em "Allowed Callback URLs / Allowed Logout URLs" do tenant.
# AUTH0_DOMAIN=
# AUTH0_AUDIENCE=
